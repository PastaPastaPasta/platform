These snapshots are release trust configuration, never downloaded replacement roots.

Mainnet height 2,400,000, block
`000000000000002883c9151a37092287c1773c5a4d175a8f688373daa631f934`:
raw block obtained from `https://api.blockchair.com/dash/raw/block/2400000`;
height, block hash and transaction root independently matched against
`https://api.blockcypher.com/v1/dash/main/blocks/2400000` on 2026-09-08.
All transactions were hashed to check the raw block's Merkle root. The committed
regression evidence checks X11 header identity, coinbase inclusion, height and both
snapshot roots. This validates the snapshot evidence, not a full mainnet history
proof generated by the new Core RPC.

Testnet height 1,548,500 comes from the real Dash Core 23.1.8 fixture in
`rs-core-proof/tests/data/short.json`; the fixture records its header and provenance.

Before changing a release snapshot, independently confirm its network and block
identity, derive both roots from the included complete coinbase, and update the
regression evidence. A remote service's self-consistent proof does not authorize a
new release trust root. Seed addresses come from the pinned `dash-network-seeds`
crate and are connection candidates only.
